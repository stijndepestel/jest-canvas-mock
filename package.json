{
  "name": "@amaron/jest-canvas-mock",
  "version": "0.0.3",
  "description": "A module to mock canvas in Jest.",
  "main": "lib/index.js",
  "scripts": {
    "test": "jest",
    "build": "babel src --out-dir lib",
    "coveralls": "cat ./coverage/lcov.info | coveralls"
  },
  "keywords": [
    "mock",
    "jest",
    "jest-mock",
    "echarts",
    "canvas",
    "test",
    "unit"
  ],
  "author": "hustcc",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-jest": "^22.0.4",
    "babel-preset-env": "^1.6.1",
    "coveralls": "^3.0.0",
    "jest": "^21.2.1"
  },
  "jest": {
    "collectCoverage": true,
    "setupFiles": [
      "./src/index.js"
    ]
  }
}
